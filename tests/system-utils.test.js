const {getHTML} = require("../src/utils/system-utils");
import 'regenerator-runtime/runtime';

describe("Tests data", () => {

    test("html", async () => {
        const html = getHTML("list-item");
    });

});